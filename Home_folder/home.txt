HTML (HyperText Markup Language) is the standard markup language used to create and structure documents 
displayed across the World Wide Web. 
It provides the skeleton of every web page, defining elements like headings, paragraphs, links, 
and embedded images so web browsers can render them properly